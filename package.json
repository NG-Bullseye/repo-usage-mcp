{
  "name": "repo-usage-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only MCP server that traces Claude Code token usage per repository. Reads ~/.claude/projects/*.jsonl, keeps Claude-Code-mode messages only (DeepSeek/synthetic excluded), and reports the four token dimensions (input, output, cache_creation, cache_read) separately, grouped by repo.",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "claude",
    "claude-code",
    "anthropic",
    "token-usage",
    "cost-tracking",
    "multi-repo"
  ],
  "author": "Leonard-Riccardo Wecke",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "repo-usage-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "test": "node --test \"test/**/*.test.mjs\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
